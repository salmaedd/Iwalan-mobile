// import { selectProductSummaryDomain } from '../selectors';

describe('selectProductSummaryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
