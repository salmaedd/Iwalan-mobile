import { createSelector } from 'reselect';
import { initialState } from './reducer';

/**
 * Direct selector to the catalog state domain
 */

// const selectCatalogDomain = state => state.catalog || initialState;

/**
 * Other specific selectors
 */

/**
 * Default selector used by Catalog
 */

// const makeSelectCatalog = () =>
//   createSelector(
//     selectCatalogDomain,
//     substate => substate,
//   );
// const makeSelectBrands = () =>
//   createSelector(
//     selectCatalogDomain,
//     substate => substate.brands,
//   );

// export default makeSelectCatalog;
// export { selectCatalogDomain, makeSelectBrands };
