// import { selectProductDetailsDomain } from '../selectors';

describe('selectProductDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
