// import { selectGiftsContainerDomain } from '../selectors';

describe('selectGiftsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
