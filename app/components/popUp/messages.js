/*
 * AlertElement Messages
 *
 * This contains all the text for the AlertElement component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AlertElement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AlertElement component!',
  },
});
