/*
 * OrderTimeline Messages
 *
 * This contains all the text for the OrderTimeline component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderTimeline';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderTimeline component!',
  },
});
