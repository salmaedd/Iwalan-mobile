/*
 * CartElement Messages
 *
 * This contains all the text for the CartElement component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CartElement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CartElement component!',
  },
});
