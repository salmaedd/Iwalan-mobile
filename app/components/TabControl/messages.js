/*
 * TabControl Messages
 *
 * This contains all the text for the TabControl component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TabControl';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabControl component!',
  },
});
