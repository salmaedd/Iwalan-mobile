/*
 * GiftCard Messages
 *
 * This contains all the text for the GiftCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GiftCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GiftCard component!',
  },
});
