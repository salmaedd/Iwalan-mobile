/*
 * TransparentCartElement Messages
 *
 * This contains all the text for the TransparentCartElement component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TransparentCartElement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransparentCartElement component!',
  },
});
