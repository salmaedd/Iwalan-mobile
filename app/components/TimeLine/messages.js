/*
 * TimeLine Messages
 *
 * This contains all the text for the TimeLine component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimeLine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimeLine component!',
  },
});
