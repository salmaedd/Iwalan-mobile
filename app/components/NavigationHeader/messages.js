/*
 * NavigationHeader Messages
 *
 * This contains all the text for the NavigationHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavigationHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavigationHeader component!',
  },
});
